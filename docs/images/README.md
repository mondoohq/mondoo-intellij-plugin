# Images

`plugin-overview.png` — the plugin at work in GoLand: findings highlighted in the
editor, grouped by severity and rule in the Mondoo tool window, with the count in the
status bar. Referenced from the project README.

Retake it on a demo file (**Tools → Mondoo Code Security → Open Demo File**) so the
findings are reproducible and nothing real is shown.
